import React from 'react';
import injectSheet from 'react-jss';
import styles from './RankUpStyles';

function RankUpPage1({ character, classes }) {
    const { pageOne } = classes

    return (
        <div></div>
    )
}

export default injectSheet(styles)(RankUpPage1);